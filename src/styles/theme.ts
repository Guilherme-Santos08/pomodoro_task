import {
  grayDark,
  greenDark,
  indigoDark,
  redDark,
  sandDark,
  violetDark,
  skyDark,
  blueDark,
  mauveDark,
  yellowDark,
} from '@radix-ui/colors'

const theme = {
  colors: {
    ...sandDark,
    ...grayDark,
    ...violetDark,
    ...indigoDark,
    ...greenDark,
    ...redDark,
    ...skyDark,
    ...blueDark,
    ...mauveDark,
    ...redDark,
    ...greenDark,
    ...yellowDark,
  },
}

export default theme
