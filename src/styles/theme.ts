import {
  grayDark,
  greenDark,
  indigoDark,
  redDark,
  sandDark,
  violetDark,
  skyDark,
  blueDark,
  mauveDark,
} from '@radix-ui/colors'

const theme = {
  colors: {
    ...sandDark,
    ...grayDark,
    ...violetDark,
    ...indigoDark,
    ...greenDark,
    ...redDark,
    ...skyDark,
    ...blueDark,
    ...mauveDark,
    ...redDark,
  },
}

export default theme
