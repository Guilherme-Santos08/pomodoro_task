import {
  grayDark,
  greenDark,
  indigoDark,
  redDark,
  sandDark,
  violetDark,
} from '@radix-ui/colors'

const theme = {
  colors: {
    ...sandDark,
    ...grayDark,
    ...violetDark,
    ...indigoDark,
    ...greenDark,
    ...redDark,
  },
}

export default theme
