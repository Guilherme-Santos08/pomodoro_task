import { grayDark, indigoDark, sandDark, violetDark } from '@radix-ui/colors'

const theme = {
  colors: {
    ...sandDark,
    ...grayDark,
    ...violetDark,
    ...indigoDark,
  },
}

export default theme
